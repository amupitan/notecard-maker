<!DOCTYPE html>
<html>

<head>
    <title>Create Notecrads</title>
    <%include partials/head.ejs%>
    <link rel="stylesheet" href="/css/form.css" type="text/css">
</head>

<body>
	<header>
			<% include partials/header.ejs %>
	</header>
	<div class="container">
   <div class ="row top-buffer">
      <div class="col-md-offset-3 col-sm-8 col-md-6" >
        <p class="h2">Upload notes:</p>
        <% if (errors) { %>
          <div class="errors alert alert-danger alert-dismissable">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
            <strong><%=errors.name%></strong> <%=errors.message%>
          </div>
        <% } %>
        <form action="/notes_upload" method="post" enctype="multipart/form-data">
            Select file to upload:
            <br />
            <label class="custom-file">
              <input type="file" name="noteFile" id="fileToUpload" class="custom-file-input">
              <span class="custom-file-control"></span>
            </label>
            <!--<input type="file"  class="custom-file-input">-->
            <br />
            <input type="submit" class="btn btn-primary" id="login" value="Upload">
        </form>
      </div>
   </div>
  </div>
  <%include partials/foot.ejs%>
</body>
</html>
